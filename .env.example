# Meta Ads API Configuration
# Required: Your Meta access token
META_ACCESS_TOKEN=your_access_token_here

# Optional: Meta API version (default: v22.0)
META_API_VERSION=v22.0

# Optional: Meta App credentials for OAuth
# META_APP_ID=your_app_id
# META_APP_SECRET=your_app_secret

# Optional: Authentication mode (token | oauth)
# AUTH_MODE=token

# Optional: Server configuration
# NODE_ENV=development
# DEBUG=false
# LOG_LEVEL=info

# Optional: Rate limiting
# RATE_LIMIT_THRESHOLD=80

# Optional: HTTP transport (if using http-server.ts)
# HTTP_PORT=3000
# ENABLE_CORS=true
# SESSION_SECRET=random_secret_string
# REDIS_URL=redis://localhost:6379
